{
  "name": "ymd-object",
  "version": "0.0.1",
  "description": "Defines a simple but handy date type called YMD to help you avoid confusing results you may get when using the standard Date object for dates (e.g. new Date(\"2020-01-01\"))",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^27.0.4",
    "ts-jest": "^27.0.2",
    "typescript": "^4.3.2"
  }
}
